<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sinoangel.ctrl.parentalcontrol">

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <!-- 友盟 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="591c328c8630f5520e000dc8" />

        <activity
            android:name=".index.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="userPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".account.LoginActivity"
            android:screenOrientation="userPortrait" />
        <activity
            android:name=".account.RegisterActivity"
            android:screenOrientation="userPortrait" />
        <activity
            android:name=".account.ForgetPWDActivity"
            android:screenOrientation="userPortrait" />
        <activity android:name=".account.SelectHeadActivity"></activity>
    </application>

</manifest>